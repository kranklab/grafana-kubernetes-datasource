apiVersion: 1

datasources:
  - name: 'kubernetes-datasource'
    type: 'kranklab-kubernetes-datasource'
    access: proxy
    isDefault: false
    orgId: 1
    version: 1
    editable: true
    jsonData:
      url: "localhost:2323"
    secureJsonData:
      apiKey: 'api-key'
